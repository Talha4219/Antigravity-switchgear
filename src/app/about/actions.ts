'use server';

// This file previously contained AI generation logic which has been deprecated.
// Keeping the file to avoid import errors if referenced elsewhere, but functionality is removed.

export async function generateAboutUsAction(input: any) {
  return { error: 'This feature is currently disabled.' };
}
